#!/usr/bin/python3
#0-lookup.py
"""Defines an object attribute, lookup function. Task 0"""


def lookup(obj):
    """Returns the list of object's available attributes && methods"""
    return (dir(obj))
